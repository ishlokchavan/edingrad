/**
 * Database types.
 *
 * Placeholder until generated against the live Supabase project:
 *
 *   supabase gen types typescript --project-id <ref> --schema public \
 *     > src/lib/supabase/types.ts
 *
 * (or the Supabase MCP `generate_typescript_types`). The schema is defined in
 * `supabase/migrations/0001_init.sql`. Until then the clients are loosely typed.
 */
export type Database = Record<string, unknown>;
